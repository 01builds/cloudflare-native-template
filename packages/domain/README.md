# 🧠 Domain Contracts Package

This package manages the business objects, validation contracts, and core TypeScript interfaces shared across the frontend client and backend gateway.

## Structure

- `src/types.ts`: Business models and structure definitions.
- `src/validators.ts`: Input validation rules managed via **Zod**.

Shared validation schemas ensure type safety and schema consistency from client forms to backend request processing middleware.
